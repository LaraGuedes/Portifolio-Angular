import { Routes } from '@angular/router';
import { HomeComponent } from './modulos/portifolio/pages/home/home.component';

export const routes: Routes = [
    {
        path:'',
        component: HomeComponent,
    }
];
